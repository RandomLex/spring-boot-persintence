server:
  port: 8080
  servlet:
    context-path: /spring_securty_it_war
spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/it
    username: alex
    password: vhXA-5

repository:
  type: memory
